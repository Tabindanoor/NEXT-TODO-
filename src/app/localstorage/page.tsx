import LocalStorage from "../../components/LocalStorage"

const page = () => {
  return (
    <div>
        <p>this is the local storage todo </p>
        <LocalStorage/>
    </div>
  )
}

export default page